// src/reportWebVitals.js

const reportWebVitals = (metric) => {
    console.log(metric);
    // You can send the metrics to an analytics endpoint, if desired
  };
  
  export default reportWebVitals;
  